import React from 'react'
import ProductsList from './products-list'

describe('products-list', function ProductsListSpec() {
  test('it exists', () => {
    expect(ProductsList).toBeDefined()
  })
})
